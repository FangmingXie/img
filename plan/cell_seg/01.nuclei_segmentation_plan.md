# Plan 01 — 3D nuclei segmentation of `cell_segmentation_Saumya.tif`

## Context

`links/cell_seg/cell_segmentation_Saumya.tif` → `/home/qlyu/mydata/data/jainlab26_img/cell_segmentation_Saumya.tif`
is a deconvolved Nikon AX confocal stack of a *Drosophila* sample
(`9B08_slp1_RNAiv20_24h_slp1_beatiia ... lobe1`). Goal: segment **nuclei** in 3D
and produce label masks, a per-cell measurement table, QC overlays, and a count
summary. GPU is plentiful (4× RTX 6000 Ada, 48 GB, idle), so a deep-learning
segmenter is the right call.

### Image facts (measured)
- Shape **ZCYX = (39, 4, 262, 266)**, `uint16`, ImageJ hyperstack, unit = micron.
- Originally 1024×1024 in the source `.nd2`; saved here downsampled to ~262×266 (XY).
- **Z-step not recorded** in metadata (`spacing: None`).
- Per-channel signal: **C0** dense/uniform (mean 78) → DAPI/nuclei; C1 bright/dense
  (mean 116); C2 sparse (mean 7); C3 very sparse (51% nonzero) — specific markers.

### Decisions (confirmed with user)
- **Target:** nuclei, on **channel C0 (DAPI)**.
- **Z handling:** Z-step unknown → segment **2D per-slice + stitch across Z**
  (`do_3D=False, stitch_threshold>0`), which needs no anisotropy. Keep true-3D
  (`do_3D=True` with an estimated anisotropy) as a fallback toggle.
- **Deliverables:** label-mask TIFF · per-cell CSV · QC overlay PNGs · count summary.

## Tool choice

**Primary: Cellpose-SAM (Cellpose v4, `cpsam` model).** Generalist, no training
needed, strong on DAPI nuclei, native 3D + 2D-stitch modes, GPU. This is the
"existing tool" the user pointed at and the best default here.

**Alternatives considered (documented, not run by default):**
- **scikit-image watershed baseline** (Gaussian → threshold → distance transform →
  `peak_local_max` → `watershed`). Uses standard scientific-Python packages
  installed into the `img` env (see `plan/cell_seg/02.watershed_baseline_plan.md`).
  Run first as a sanity check / fallback if the Cellpose install slips.
- **StarDist 2D `2D_versatile_fluo` + Z-stitch** — excellent for round, densely
  packed fluorescent nuclei; good second opinion if Cellpose over/under-merges.
  (StarDist *3D* would need training data, so we use the 2D pretrained model per-slice.)
- **InstanSeg** (2024) — fast nuclei/cell model with 3D support; mention only.

Recommendation: start with the **watershed baseline** for an immediate look, then
**Cellpose-SAM** as the production result; reach for **StarDist 2D+stitch** only if
Cellpose quality is unsatisfactory.

## Prerequisite

The `img` env must have the scientific stack (Group A) **and** Cellpose + GPU
PyTorch (Group B) installed — see **`plan/cell_seg/00.package_install_plan.md`**,
which is the single place package installation is defined. Verify GPU before the
heavy run: `conda run -n img python -c "import torch; print(torch.cuda.is_available())"`.

## Cellpose API (as installed — cellpose 4.1.1, verified)

- **Model:** `from cellpose import models; model = models.CellposeModel(gpu=True)`.
  Default `pretrained_model='cpsam'` (Cellpose-SAM). The old `models.Cellpose`
  class **no longer exists** — do not use `model_type=...`.
- **Eval:** `masks, flows, styles = model.eval(x, ...)` — returns **3** values
  (no `diams`). Relevant kwargs and their defaults:
  `z_axis=None, channel_axis=None, normalize=True, diameter=None,
  flow_threshold=0.4, cellprob_threshold=0.0, do_3D=False, anisotropy=None,
  stitch_threshold=0.0, min_size=15, batch_size=8`.
- **Single-channel input:** pass the C0 volume as `(Z, Y, X)` with `z_axis=0` and
  `channel_axis=None`; cpsam handles grayscale internally (the old `channels=[...]`
  arg is obsolete for cpsam). `normalize=True` lets cpsam percentile-normalize.
- **Diameter:** cpsam is trained at `diam_mean≈30 px`; `diameter=None` uses that.
  Nuclei here span ~30–40 px in XY (from the watershed QC), so `None` should be
  close; provide an explicit `DIAMETER` knob for tuning.
- **2D+stitch (primary):** `do_3D=False, stitch_threshold=0.4, z_axis=0` — segments
  each Z-slice in 2D, links across Z by mask IoU. No anisotropy needed (matches the
  unknown-Z decision). **3D fallback:** `do_3D=True, anisotropy=ANISOTROPY, z_axis=0`.

## Pipeline — scripts under `scripts/cell_seg/` (numbered, paths CAPS at top)

All read `links/cell_seg/cell_segmentation_Saumya.tif`; write results to
`local_data/res/cell_seg/` and figures to `local_data/fig/cell_seg/`. Each script
defines `PROJECT_ROOT` / `INPUT_FILE` / `OUT_*` at the top (repo convention) and
fails fast on missing inputs. Run with `conda run --no-capture-output -n img python -u ...`.

**`01.preview_channels.py`** — load stack, write per-channel max-Z-projection PNGs
(plus a mid-Z-slice grid) to `fig/cell_seg/` so channel identities (esp. confirming
C0=DAPI) are visually verified. Also print the per-channel intensity stats table.

**Two segmentation scripts** (2D and 3D kept separate, per request), both reading
C0 `(Z,Y,X)` as float32, building `model = CellposeModel(gpu=True)`, applying
per-Z percentile normalization (counters depth dimming), saving a relabelled
ImageJ-compatible TIFF, and printing elapsed time + nucleus count. Shared config
defaults: `DIAMETER=None`, `FLOW_THRESHOLD=0.4`, `CELLPROB_THRESHOLD=-1.0`
(lower → recover dimmer/deeper nuclei), `MIN_SIZE=50`, `BATCH_SIZE=16`,
`PER_Z_NORM=True` (so `normalize=False` is passed to `eval`).

- **`02.segment_nuclei_cellpose.py`** — 2D per-slice + Z-stitch
  (`do_3D=False, stitch_threshold=0.4, z_axis=0`). No anisotropy needed.
  → `02.nuclei_labels_cellpose.tif`. **As-run: 50 nuclei, 5.3 s.** Cleaner contours;
  tends to lose nuclei in dim/blurry deep slices.
- **`02c.segment_nuclei_cellpose_3d.py`** — true 3D
  (`do_3D=True, anisotropy=3.0, z_axis=0`). → `02c.nuclei_labels_cellpose3d.tif`.
  **As-run: 130 nuclei, 24.6 s.** Recovers deep nuclei but over-fragments — sensitive
  to the `ANISOTROPY` guess (unknown Z-step); needs the true Z:XY to be trustworthy.

**`03.measure_and_qc.py`** — *already implemented* (Plan 02); generic over any label
TIFF via a CLI arg. Reuse as-is:
`conda run -n img python -u scripts/cell_seg/03.measure_and_qc.py <OUT_LABELS>`
→ writes `03.nuclei_measurements_<tag>.csv`, `03.count_summary_<tag>.txt`, and QC
overlays, where `<tag>` is derived from the label filename (e.g. `cellpose`).
(Minor: improve the tag derivation to the last `_`-token so
`02.nuclei_labels_cellpose.tif` → tag `cellpose`.)

**`02b.segment_watershed.py`** — the classical baseline (Plan 02), already
implemented; gives the comparison mask (`tag=watershed`).

## Tuning knobs (expected iteration, most → least impactful)
1. `DIAMETER` — if cpsam under/over-merges, set to the measured XY nucleus diameter.
2. `STITCH_THRESHOLD` — raise to split Z-merged nuclei, lower to connect fragments.
3. `CELLPROB_THRESHOLD` (lower = more/larger masks) and `FLOW_THRESHOLD`
   (higher = keep more, less shape-strict) — recall vs precision.
4. Run `02c` (true 3D) with a corrected `ANISOTROPY` if 2D+stitch under-merges along
   Z — but it needs the real Z:XY ratio to avoid over-fragmentation.

## Open items to resolve during execution
- **Confirm C0 = DAPI** via `01.preview_channels.py` before trusting the masks.
- **Voxel size**: XY ≈ 0.0575 µm/px is recorded but likely stale post-downsample;
  Z-step unrecorded. Volumes start in **voxels**; convert to µm³ only once the user
  supplies true XY/Z spacing.

## Verification
1. `conda run -n img python -c "import cellpose, torch; print(cellpose.version, torch.cuda.is_available())"`
   → `4.1.1 True` (already confirmed).
2. Run `01.preview_channels.py` → eyeball channel PNGs; confirm C0 = DAPI.
3. Run `02.segment_nuclei_cellpose.py` on the full stack → `02.nuclei_labels_cellpose.tif`.
4. Run `03.measure_and_qc.py 02.nuclei_labels_cellpose.tif` → check CSV row count ==
   nucleus count and that QC overlay boundaries track visible nuclei.
5. Compare against the `02b` watershed baseline (72 nuclei): counts + QC overlays
   side by side; iterate `DIAMETER`/`STITCH_THRESHOLD` if needed.
