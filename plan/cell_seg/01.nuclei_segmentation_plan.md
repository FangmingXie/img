# Plan 01 — 3D nuclei segmentation of `cell_segmentation_Saumya.tif`

## Context

`links/cell_seg/cell_segmentation_Saumya.tif` → `/home/qlyu/mydata/data/jainlab26_img/cell_segmentation_Saumya.tif`
is a deconvolved Nikon AX confocal stack of a *Drosophila* sample
(`9B08_slp1_RNAiv20_24h_slp1_beatiia ... lobe1`). Goal: segment **nuclei** in 3D
and produce label masks, a per-cell measurement table, QC overlays, and a count
summary. GPU is plentiful (4× RTX 6000 Ada, 48 GB, idle), so a deep-learning
segmenter is the right call.

### Image facts (measured)
- Shape **ZCYX = (39, 4, 262, 266)**, `uint16`, ImageJ hyperstack, unit = micron.
- Originally 1024×1024 in the source `.nd2`; saved here downsampled to ~262×266 (XY).
- **Z-step not recorded** in metadata (`spacing: None`).
- Per-channel signal: **C0** dense/uniform (mean 78) → DAPI/nuclei; C1 bright/dense
  (mean 116); C2 sparse (mean 7); C3 very sparse (51% nonzero) — specific markers.

### Decisions (confirmed with user)
- **Target:** nuclei, on **channel C0 (DAPI)**.
- **Z handling:** Z-step unknown → segment **2D per-slice + stitch across Z**
  (`do_3D=False, stitch_threshold>0`), which needs no anisotropy. Keep true-3D
  (`do_3D=True` with an estimated anisotropy) as a fallback toggle.
- **Deliverables:** label-mask TIFF · per-cell CSV · QC overlay PNGs · count summary.

## Tool choice

**Primary: Cellpose-SAM (Cellpose v4, `cpsam` model).** Generalist, no training
needed, strong on DAPI nuclei, native 3D + 2D-stitch modes, GPU. This is the
"existing tool" the user pointed at and the best default here.

**Alternatives considered (documented, not run by default):**
- **scikit-image watershed baseline** (Gaussian → threshold → distance transform →
  `peak_local_max` → `watershed`). Uses standard scientific-Python packages
  installed into the `img` env (see `plan/cell_seg/02.watershed_baseline_plan.md`).
  Run first as a sanity check / fallback if the Cellpose install slips.
- **StarDist 2D `2D_versatile_fluo` + Z-stitch** — excellent for round, densely
  packed fluorescent nuclei; good second opinion if Cellpose over/under-merges.
  (StarDist *3D* would need training data, so we use the 2D pretrained model per-slice.)
- **InstanSeg** (2024) — fast nuclei/cell model with 3D support; mention only.

Recommendation: start with the **watershed baseline** for an immediate look, then
**Cellpose-SAM** as the production result; reach for **StarDist 2D+stitch** only if
Cellpose quality is unsatisfactory.

## Prerequisite

The `img` env must have the scientific stack (Group A) **and** Cellpose + GPU
PyTorch (Group B) installed — see **`plan/cell_seg/00.package_install_plan.md`**,
which is the single place package installation is defined. Verify GPU before the
heavy run: `conda run -n img python -c "import torch; print(torch.cuda.is_available())"`.

## Pipeline — scripts under `scripts/cell_seg/` (numbered, paths CAPS at top)

All read `links/cell_seg/cell_segmentation_Saumya.tif`; write results to
`local_data/res/cell_seg/` and figures to `local_data/fig/cell_seg/`. Each script
defines `PROJECT_ROOT` / `INPUT_FILE` / `OUT_*` at the top (repo convention) and
fails fast on missing inputs.

**`01.preview_channels.py`** — load stack, write per-channel max-Z-projection PNGs
to `fig/cell_seg/` so channel identities (esp. confirming C0=DAPI) are visually
verified before committing. Also print per-channel stats. (Runs in the `img` env;
needs only tifffile + matplotlib from the install step.)

**`02.segment_nuclei_cellpose.py`** — core segmentation:
- Read C0 volume `(Z,Y,X)`; percentile-normalize (cpsam also normalizes internally).
- `from cellpose import models; model = models.CellposeModel(gpu=True)` (cpsam).
- Tune **diameter** on a mid-Z slice first (try auto + a couple of values), then run
  full volume with **`do_3D=False, stitch_threshold=0.4`** (2D+stitch).
  Expose `DO_3D`, `ANISOTROPY`, `DIAMETER`, `FLOW_THRESHOLD`, `CELLPROB_THRESHOLD`,
  `STITCH_THRESHOLD` as top-of-file config constants.
- Save label mask as `res/cell_seg/02.nuclei_labels.tif` (ImageJ-compatible uint16/32).
- Note: Cellpose v4 uses `CellposeModel.eval` (the old `Cellpose(model_type=...)`
  API is deprecated) — confirm exact signature against the installed version on first run.

**`03.measure_and_qc.py`** — quantify + QC:
- `skimage.measure.regionprops_table` on the label mask → per-cell CSV
  `res/cell_seg/03.nuclei_measurements.csv` with label, centroid (z,y,x), volume
  (voxels; µm³ if voxel size supplied later), and **mean intensity in each of C0–C3**.
- Count summary (total nuclei + volume distribution) printed and saved to
  `res/cell_seg/03.count_summary.txt`.
- QC overlays to `fig/cell_seg/`: mask boundaries over C0 for a few Z-slices and a
  max-projection (e.g. via `skimage.segmentation.find_boundaries`).

**Optional `02b.segment_watershed.py`** — scikit-image baseline (runs in `img`)
producing a comparison label mask for sanity-checking. Detailed in
`plan/cell_seg/02.watershed_baseline_plan.md`.

## Tuning knobs (expected iteration)
- `diameter` (most impactful) — estimate from preview; nuclei look small in the
  downsampled field. `stitch_threshold` (Z-merge strength). `flow_threshold` /
  `cellprob_threshold` (recall vs precision). `do_3D=True`+`anisotropy` if stitch
  under-merges along Z — but that needs the real Z-step.

## Open items to resolve during execution
- **Confirm C0 = DAPI** via `01.preview_channels.py` before trusting the masks.
- **Voxel size**: XY ≈ 0.0575 µm/px is recorded but likely stale post-downsample;
  Z-step unrecorded. Volumes start in **voxels**; convert to µm³ only once the user
  supplies true XY/Z spacing.

## Verification
1. `conda run -n img python -c "import cellpose, torch; print(cellpose.version, torch.cuda.is_available())"`.
2. Run `01.preview_channels.py` → eyeball channel PNGs; confirm DAPI channel.
3. Run `02.segment_nuclei_cellpose.py` on the full stack; open
   `02.nuclei_labels.tif` in napari/ImageJ alongside C0.
4. Run `03.measure_and_qc.py`; sanity-check count + CSV row count match, and that
   QC overlay boundaries track visible nuclei.
5. Compare against `02b` watershed baseline; iterate `diameter`/thresholds if needed.
